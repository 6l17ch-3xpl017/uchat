#include "server.h"

void create_new_empty_chat(json_t *income_json, struct ns_connection *socket) {
//    t_chat *Chat = malloc(sizeof(t_chat));
//    json_t *chat;
//    char *chat_type;
//
//    chat_type = strdup(json_string_value(json_object_get(user_message, "chat_type")));

//    chat = json_object_get(income_json, "chat");
//    if (!json_is_object(chat)) {
//        User->nickname = NULL;
//        send_status(User, Chat, socket->socket, unknown_error, "sign_up");
//        json_decref(user);
//        return 0;
//    }

//    if (strcmp(chat_type, "public") == 0) {
//        Chat->chat_name = strdup(json_string_value(json_object_get(user_message, "chat_name")));
//        Chat->admin_id = strdup(json_string_value(json_object_get(user_message, "admin_id")));
//    }
    //todo add user to chat
}

