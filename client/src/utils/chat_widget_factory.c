#include "client.h"

GtkWidget *chat_widget_factory(json_t *chat_obj)
{
//    char *chat_name, chat_id;
//    json_unpack(chat_obj, "{s:s, s:s}" "chat_name", &chat_name, "chat_id", &chat_id);
//
//    GtkWidget *chat = gtk_label_new(chat_name);
//    g_object_set_data(G_OBJECT(chat), "chat_id", chat_id); // ToDo: Test it
//
//    json_decref(chat_obj);
//
//    return chat;
}
